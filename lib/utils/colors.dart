import 'package:flutter/material.dart';

const black0Color = Color.fromRGBO(0, 0, 0, 1); // Mobile Background Color
const black18Color = Color.fromRGBO(18, 18, 18, 1); // Web Background Color
const black38Color = Color.fromRGBO(38, 38, 38, 1); // Search Color
const blueColor = Color.fromRGBO(0, 149, 246, 1);
const whiteColor = Colors.white;
const greyColor = Colors.grey;